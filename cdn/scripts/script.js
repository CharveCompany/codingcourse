// JavaScript source code
// Credits to CharveCompany
function forcequit() {

        if (confirm("Are you sure to exit? This might cause loss data!")) {
            window.close();
        }
}